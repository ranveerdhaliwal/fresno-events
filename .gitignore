node_modules
dist
.turbo
.wrangler
.env
.env.*
!.env.example
*.local
coverage
supabase/.branches
supabase/.temp
apps/web/src/routeTree.gen.ts
*.tsbuildinfo

# Wrangler local secrets (API + ingest, all env profiles)
**/.dev.vars
**/.dev.vars.*
!**/.dev.vars.example

# Cloud target URLs for `pnpm dev:web:cloud-{dev,prod}` (gitignored convenience)
.env.cloud-targets

# Dev Supabase toggle (local vs cloud-dev vs cloud-prod) — see dev-target.env.example
dev-target.env

# Cursor: track shared project rules; keep local MCP/settings private
.cursor/*
!.cursor/rules/
!.cursor/rules/**
