# AUTO-GENERATED by `pnpm env:local|cloud-dev|cloud-prod` from repo-root dev-target.env.
# Do not copy this file to .dev.vars — run pnpm env:<target> after filling dev-target.env.
#
#   cp dev-target.env.example dev-target.env
#   pnpm env:local
#
# Keys for this worker (set in dev-target.env) — see dev-target.env.example for the full list.
# Supabase URL + service role come from DEV_TARGET (local / cloud-dev / cloud-prod).
#
# Do not put BR / LLM keys in apps/api — API worker does not use them.
